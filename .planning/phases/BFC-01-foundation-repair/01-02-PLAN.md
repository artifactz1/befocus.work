---
phase: 01-foundation-repair
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/next/package.json
  - bun.lock
  - packages/ui/src/globals.css
  - packages/ui/tailwind.config.ts
autonomous: true
requirements: [FND-02, FND-03]

must_haves:
  truths:
    - "D-01: apps/next declares `tailwindcss` with the exact same range as packages/ui (`^3.4.13`); the unrelated `tailwind` package is gone from package.json, bun.lock and node_modules resolution for apps/next"
    - "D-02: globals.css contains no `@theme` block (v4-only syntax Tailwind 3 silently drops), and `shine` is a real Tailwind 3 keyframes + animation, so `motion-safe:animate-shine` compiles to CSS that references an existing `@keyframes shine`"
    - "D-03: `--border` is defined exactly once for `:root` (`0 0% 69%`) and once for `.dark` (`0 0% 25%`), the values that render today, so the dashboard looks identical"
    - "The dashboard renders with zero visual change: computed `--border` on <html> is `0 0% 25%` before and after, and the compiled CSS diff contains only the intended token/keyframe changes"
  artifacts:
    - path: "apps/next/package.json"
      provides: "Real Tailwind 3 dependency"
      contains: "\"tailwindcss\": \"^3.4.13\""
    - path: "packages/ui/tailwind.config.ts"
      provides: "shine keyframes and animation"
      contains: "shine"
    - path: "packages/ui/src/globals.css"
      provides: "Single-sourced design tokens, Tailwind 3 compatible"
  key_links:
    - from: "packages/ui/src/components/magicui/shine-border.tsx"
      to: "packages/ui/tailwind.config.ts theme.extend.animation.shine"
      via: "motion-safe:animate-shine utility class generated by Tailwind 3 content scan"
      pattern: "animate-shine"
---

## Phase Goal

The dashboard receives a signed-in user's saved settings on the server render in production, from
exactly one hydration path, and the styling and lint toolchain is single-sourced and trustworthy again.

<objective>
Slice: the app builds against one declared Tailwind (v3.4) and ships no CSS that Tailwind silently
ignores or that defines a token twice - with zero visual change to the dashboard.

Purpose: FND-02 + FND-03 share one decision (D-01: stay on v3). Phase 2 writes CSS custom properties onto
the root, so token definitions must be single-sourced first.
Output: fixed `apps/next/package.json`, regenerated `bun.lock`, cleaned `globals.css`, `shine` in
`tailwind.config.ts`.

Serialized after 01-01 only because both plans run `next dev`/`next build` in the same `apps/next`
(shared `.next` dir and `node_modules`); there is no logical dependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/BFC-01-foundation-repair/01-CONTEXT.md
@.planning/phases/BFC-01-foundation-repair/01-PATTERNS.md
@CLAUDE.md

<interfaces>
apps/next/package.json devDependencies today: `"tailwind": "^4.0.0"` (unrelated streaming lib).
packages/ui/package.json: `"tailwindcss": "^3.4.13"`. apps/next/tailwind.config.ts and
apps/next/postcss.config are one-line re-exports of the packages/ui versions.

packages/ui/src/globals.css `--border` definitions (line numbers approximate):
- ~57 inside `@layer base { :root {` -> `--border: 60 4% 80%;` (shadowed today)
- ~87 inside `@layer base { .dark {` -> `--border: 12 6.5% 15.1%;` (shadowed today)
- ~96 `.theme { --animate-shine: shine var(--duration) infinite linear; }` (orphan v4-style var, nothing reads it)
- ~203 unlayered `:root { --border: 0 0% 69%; }` (wins today)
- ~207 unlayered `.dark { --border: 0 0% 25%; }` (wins today; <html> is always `dark`)
- ~211-223 `@theme inline { @keyframes shine { 0% {background-position: 0% 0%} 50% {background-position: 100% 100%} to {background-position: 0% 0%} } }`

packages/ui/tailwind.config.ts existing pattern (theme.extend):
```typescript
keyframes: {
  'accordion-down': { from: { height: '0' }, to: { height: 'var(--radix-accordion-content-height)' } },
  'accordion-up': { from: { height: 'var(--radix-accordion-content-height)' }, to: { height: '0' } },
},
animation: {
  'accordion-down': 'accordion-down 0.2s ease-out',
  'accordion-up': 'accordion-up 0.2s ease-out',
},
```
Only `shine` consumer: packages/ui/src/components/magicui/shine-border.tsx (`motion-safe:animate-shine`,
sets `--duration` inline). It is not rendered anywhere in apps/next today, so verification is on compiled
CSS, not on screen.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the failing baseline, then replace the bogus tailwind dependency (D-01)</name>
  <files>apps/next/package.json, bun.lock</files>
  <read_first>
    - apps/next/package.json
    - packages/ui/package.json
    - apps/next/tailwind.config.ts, apps/next/postcss.config.* (re-exports)
  </read_first>
  <action>
Baseline first (this is the "failing" state):
1. `bun run --cwd apps/next build`; record pass/fail. If it fails for reasons unrelated to Tailwind,
   record the error in the SUMMARY and continue (plan 01-04 owns the full build gate).
2. Save the compiled CSS for later diffing: concatenate `apps/next/.next/static/css/*.css` into
   `<scratchpad>/css-before.css`. Record `grep -o -- '--border:[^;}]*' <file>` output (expect 4 hits) and
   `grep -c '@keyframes shine' <file>` (expect 0 - proves Tailwind 3 dropped the `@theme` block).

Fix per D-01: in `apps/next/package.json` devDependencies delete `"tailwind": "^4.0.0"` and add
`"tailwindcss": "^3.4.13"` (byte-identical range to packages/ui so versions stay consistent). Keep keys
alphabetical. Run `bun install` from the repo root so bun.lock is regenerated by the tool (never hand-edit
bun.lock). Do not stage the pre-existing unrelated `package-lock.json` modification.

Note: `bun run check-deps` is currently broken (`check-dependency-version-consistency: command not
found` - the tool is not a declared dependency). Do not add it in this plan; verify consistency with the
grep below and mention the broken script in the SUMMARY as a follow-up.
  </action>
  <verify>
    <automated>grep -q '"tailwindcss": "^3.4.13"' apps/next/package.json && ! grep -q '"tailwind":' apps/next/package.json && ! grep -q '"tailwind@' bun.lock && bun pm ls 2>/dev/null | grep -q 'tailwindcss@3' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - apps/next/package.json contains `"tailwindcss": "^3.4.13"` and no `"tailwind":` key
    - `grep -c '"tailwindcss": "^3.4.13"' packages/ui/package.json apps/next/package.json` shows 1 in each
    - bun.lock contains no `"tailwind@` entry (the unrelated package is fully removed)
    - `<scratchpad>/css-before.css` exists and baseline counts are recorded in the SUMMARY
  </acceptance_criteria>
  <done>apps/next depends on real Tailwind 3.4, lockfile regenerated by bun.</done>
</task>

<task type="auto">
  <name>Task 2: Remove v4-only CSS, move shine into the Tailwind 3 config, single-source --border (D-02, D-03)</name>
  <files>packages/ui/src/globals.css, packages/ui/tailwind.config.ts</files>
  <read_first>
    - packages/ui/src/globals.css
    - packages/ui/tailwind.config.ts
    - packages/ui/src/components/magicui/shine-border.tsx
  </read_first>
  <action>
Per D-02:
- Delete the whole `@theme inline { ... }` block at the end of globals.css.
- Delete the orphan `.theme { --animate-shine: ... }` rule inside `@layer base` (v4 theme-variable naming;
  nothing reads it once the config owns the animation).
- In packages/ui/tailwind.config.ts `theme.extend.keyframes` add `shine` with the same three stops
  (`0%` and `to`: backgroundPosition `0% 0%`; `50%`: backgroundPosition `100% 100%`), and in
  `theme.extend.animation` add `shine: 'shine var(--duration) infinite linear'`, following the
  accordion entries' shape.

Per D-03 (zero visual change - keep the values that win today):
- In the `@layer base` `:root` block set `--border: 0 0% 69%;` and in the `@layer base` `.dark` block set
  `--border: 0 0% 25%;` (replacing the shadowed values).
- Delete the trailing unlayered `:root { --border ... }` and `.dark { --border ... }` blocks.
- Leave every other token untouched (including `--input`).

Prove it:
1. `bun run --cwd apps/next build` must succeed. Concatenate `.next/static/css/*.css` into
   `<scratchpad>/css-after.css`.
2. `grep -o -- '--border:[^;}]*'` on css-after shows exactly two hits: `0 0% 69%` and `0 0% 25%`.
3. css-after contains `@keyframes shine` and an `animate-shine` rule referencing `shine`.
4. css-after contains no `@theme`.
5. Diff the two CSS files after splitting on `}` (e.g. `tr '}' '\n'` then `diff`): the only differences
   are the removed `--border` duplicates / `--animate-shine`, the new shine keyframes/utility, and asset
   hash noise. Paste the diff summary in the SUMMARY.
6. Runtime zero-visual-change check: run `bun run web` and `bun run api`, open `http://localhost:3000/guest`
   with agent-browser (`agent-browser skills get core` for syntax), evaluate
   `getComputedStyle(document.documentElement).getPropertyValue('--border').trim()` -> must be `0 0% 25%`,
   and take a screenshot into the scratchpad for the SUMMARY. Stop dev servers afterwards.
Commit as `fix(ui): single-source Tailwind 3 and design tokens`.
  </action>
  <verify>
    <automated>! grep -q '@theme' packages/ui/src/globals.css && test "$(grep -v '^\s*/\*' packages/ui/src/globals.css | grep -c -- '--border:')" = 2 && grep -q "shine:" packages/ui/tailwind.config.ts && bun run --cwd apps/next build >/dev/null && cat apps/next/.next/static/css/*.css | grep -q '@keyframes shine' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@theme' packages/ui/src/globals.css` prints 0
    - `grep -c -- '--animate-shine' packages/ui/src/globals.css` prints 0
    - globals.css has exactly 2 `--border:` declarations, values `0 0% 69%` (`:root`) and `0 0% 25%` (`.dark`)
    - tailwind.config.ts has `shine` under both `keyframes` and `animation`
    - `bun run --cwd apps/next build` exits 0; compiled CSS contains `@keyframes shine`, exactly two `--border:` values, no `@theme`
    - Computed `--border` on <html> at /guest is `0 0% 25%`
  </acceptance_criteria>
  <done>Tailwind 3 compiles every rule in globals.css, tokens are defined once, dashboard unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> repo | `bun install` resolves packages into the lockfile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | bun install of `tailwindcss@^3.4.13` | accept | Not a new package: the identical name and range are already declared by packages/ui and resolved in bun.lock today (hoisted 3.4.17); the change removes an unrelated package rather than adding one. No new-package legitimacy review needed |
| T-01-06 | Tampering | bun.lock | mitigate | Lockfile changes only via `bun install`; reviewer confirms the diff removes `tailwind@` and adds no unexpected packages |
</threat_model>

<verification>
- apps/next builds against declared tailwindcss 3.4.x.
- Compiled CSS: one `--border` per scope, real `@keyframes shine`, no `@theme`.
- Computed `--border` unchanged at runtime.
</verification>

<success_criteria>
Roadmap success criterion 4 holds: single declared Tailwind version, no silently ignored rule, no
duplicated conflicting token.
</success_criteria>

<output>
Create `.planning/phases/BFC-01-foundation-repair/01-02-SUMMARY.md` when done.
</output>
