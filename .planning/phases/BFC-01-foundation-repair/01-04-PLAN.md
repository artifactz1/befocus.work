---
phase: 01-foundation-repair
plan: 04
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - package.json
  - biome.json
  - bun.lock
  - apps/**/*.{ts,tsx,js,json,css} (Biome format/import churn and lint fixes)
  - packages/**/*.{ts,tsx,js,json,css} (Biome format/import churn and lint fixes; generated drizzle files excluded)
autonomous: true
requirements: [FND-04]

must_haves:
  truths:
    - "D-04: The repo runs Biome 2; `biome.json` is the `biome migrate` output (no v1 `files.ignore` / top-level `organizeImports`), and the installed platform binary version equals the `@biomejs/biome` version on macOS"
    - "D-04: bun.lock pins the Biome 2 platform binaries for both macOS (darwin-arm64) and Linux (linux-x64) at the same version"
    - "D-05: `bun run check` exits 0 with every finding fixed, not disabled: no new `biome-ignore` comments and no rules switched off beyond what `biome migrate` emitted"
    - "D-05: Mechanical format/import churn is its own commit, separate from the Biome upgrade commit and from real code fixes"
    - "Roadmap criterion 3: from a fresh `bun install --frozen-lockfile`, `bun run check` and `bun run turbo:build` both succeed"
  artifacts:
    - path: "biome.json"
      provides: "Biome 2 config"
      contains: "biomejs.dev/schemas/2"
    - path: "package.json"
      provides: "Biome 2 dependency and Biome 2 CLI scripts"
      contains: "\"@biomejs/biome\": \"^2"
  key_links:
    - from: "package.json scripts.fix:check"
      to: "Biome 2 assist (organize imports)"
      via: "biome check with formatter/linter disabled and assist enabled"
      pattern: "assist-enabled"
---

## Phase Goal

The dashboard receives a signed-in user's saved settings on the server render in production, from
exactly one hydration path, and the styling and lint toolchain is single-sourced and trustworthy again.

<objective>
Slice: `bun run check` becomes a trustworthy gate again on a fresh install, on macOS and Linux.

Purpose: FND-04. Runs last among code plans so the one-time format/import churn covers the final code of
01-01..01-03 and every later phase starts from a clean lint baseline.
Output: Biome 2 upgrade commit, mechanical churn commit, lint-fix commit, fresh-install proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/BFC-01-foundation-repair/01-CONTEXT.md
@.planning/phases/BFC-01-foundation-repair/01-PATTERNS.md
@.planning/codebase/STACK.md
@CLAUDE.md

<interfaces>
Root package.json today: `"@biomejs/biome": "^1.9.4"` under `dependencies`; scripts:
- `"check": "npm run format:check && npm run lint && npm run fix:check"`
- `"format:check": "bun x @biomejs/biome format ./packages ./apps"`
- `"lint": "bun x @biomejs/biome lint ./packages ./apps"`
- `"fix:check": "bun x @biomejs/biome check --formatter-enabled=false --linter-enabled=false --organize-imports-enabled=true ./packages ./apps"`
Other Biome scripts: packages/ui `"lint": "bun x @biomejs/biome lint ./src"`, apps/next `"lint": "biome lint"`,
packages/api `"validate": "biome ."` and `"fix": "biome --fix . && prettier ..."` (v1-era, likely broken).
biome.json: `$schema` 1.3.3, `files.ignore` list (18 entries), top-level `organizeImports`.
Style that must be preserved: single quotes, no semicolons, 100 cols, trailing commas all, JSX single quotes.
Root cause of today's failure: bun.lock pins only cli-linux-x64 / cli-linux-x64-musl 1.9.4; macOS resolved
cli-darwin-arm64 2.4.13 unpinned. Baseline `biome-ignore` count in tracked apps/packages files: 0.
Generated files that must never be reformatted: packages/api/src/db/drizzle/** (drizzle-kit SQL + meta
JSON snapshots/journal), apps/next/next-env.d.ts if present, any CHANGELOG.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade to Biome 2, run biome migrate, fix scripts and exclusions (D-04)</name>
  <files>package.json, biome.json, bun.lock, packages/ui/package.json, apps/next/package.json, packages/api/package.json</files>
  <read_first>
    - package.json
    - biome.json
    - packages/ui/package.json, apps/next/package.json, packages/api/package.json (Biome scripts)
    - .planning/codebase/STACK.md (Biome split-install notes)
  </read_first>
  <action>
1. Baseline: run `bun run check`, record the failure (expected: config/schema error) in the SUMMARY.
2. From the repo root: `bun add @biomejs/biome@^2` (keeps it in root `dependencies`; lockfile changes only
   via bun). Confirm `bun x @biomejs/biome --version` prints 2.x and that
   `node_modules/@biomejs/cli-darwin-arm64/package.json` version equals `node_modules/@biomejs/biome/package.json` version.
3. `bun x @biomejs/biome migrate --write`. Do not hand-rewrite the keys migrate handles. Then add
   negated `files.includes` entries so generated files are never touched: `!**/src/db/drizzle`
   (covers SQL and meta JSON), `!**/next-env.d.ts`, `!**/CHANGELOG.md`. If Biome 2 fails to parse
   `packages/ui/src/globals.css` on `@tailwind`/`@apply`, enable the Biome 2 CSS parser option for Tailwind
   directives (`css.parser.tailwindDirectives: true`) - that is parser config, not a disabled rule.
   Keep formatter settings identical to today (quote style, semicolons asNeeded, lineWidth 100, trailing
   commas all, JSX single quotes).
4. Scripts: check `bun x @biomejs/biome check --help` for Biome 2 flag names, then set root `fix:check` to
   `bun x @biomejs/biome check --formatter-enabled=false --linter-enabled=false --assist-enabled=true ./packages ./apps`
   (adjust only if the help output names differ). Keep `format:check` and `lint` unless flags changed.
   Leave `check`'s `npm run` chaining as is. Update package-level Biome scripts so they run under Biome 2:
   packages/ui `lint` is fine; apps/next `lint` -> `bun x @biomejs/biome lint .`; packages/api `validate`
   -> `bun x @biomejs/biome check .` and remove the broken prettier-based `fix` script only if prettier is
   not installed (CLAUDE.md: no Prettier) - replace with `bun x @biomejs/biome check --write .`.
5. Verify config loads: `bun run format:check`, `bun run lint`, `bun run fix:check` may still report
   findings but must not print configuration/schema errors.
6. Commit only package.json files, biome.json and bun.lock: `chore(biome): upgrade to Biome 2`.
  </action>
  <verify>
    <automated>bun x @biomejs/biome --version | grep -q 'Version: 2' && test "$(node -p "require('./node_modules/@biomejs/biome/package.json').version")" = "$(node -p "require('./node_modules/@biomejs/cli-darwin-arm64/package.json').version")" && grep -q '"@biomejs/cli-darwin-arm64@2' bun.lock && grep -q '"@biomejs/cli-linux-x64@2' bun.lock && grep -q 'schemas/2' biome.json && ! grep -q 'organize-imports-enabled' package.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `bun x @biomejs/biome --version` reports 2.x; darwin-arm64 binary version equals the biome package version
    - bun.lock contains `@biomejs/cli-darwin-arm64@2` and `@biomejs/cli-linux-x64@2` entries at the same version
    - biome.json `$schema` is a 2.x schema; no `files.ignore`, no top-level `organizeImports`; drizzle dir excluded
    - No script in any package.json uses `--organize-imports-enabled`
    - Running the three scripts prints no configuration errors
    - Commit touches only package.json files, biome.json, bun.lock
  </acceptance_criteria>
  <done>Biome 2 installed consistently per platform with a migrated config.</done>
</task>

<task type="auto">
  <name>Task 2: Mechanical format and import-order churn as its own commit (D-05)</name>
  <files>apps/**, packages/** (format/import order only)</files>
  <read_first>
    - biome.json (as migrated in Task 1)
  </read_first>
  <action>
Run only the mechanical writers:
`bun x @biomejs/biome format --write ./packages ./apps` then
`bun x @biomejs/biome check --write --formatter-enabled=false --linter-enabled=false --assist-enabled=true ./packages ./apps`.
Do not run lint `--write` here. Confirm `git diff --name-only` lists nothing under
`packages/api/src/db/drizzle/` and no CHANGELOG/next-env.d.ts. Spot-check `git diff --stat` and two or
three diffs to confirm only whitespace/quotes/commas/import order changed. Run
`bun x tsc --noEmit -p apps/next` to confirm nothing broke. Commit alone:
`style: apply Biome 2 formatting and import order`.
  </action>
  <verify>
    <automated>bun run format:check && bun run fix:check && test -z "$(git show --name-only --format= HEAD | grep -E 'src/db/drizzle/|CHANGELOG|next-env.d.ts')" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `bun run format:check` exits 0
    - `bun run fix:check` exits 0
    - The churn commit contains no file under packages/api/src/db/drizzle/, no CHANGELOG.md, no next-env.d.ts
    - The churn commit contains no package.json/biome.json/bun.lock changes
    - `bun x tsc --noEmit -p apps/next` exits 0
  </acceptance_criteria>
  <done>Formatting and import order are clean in a standalone commit.</done>
</task>

<task type="auto">
  <name>Task 3: Fix every lint finding by hand, then prove the gate from a fresh install (D-05)</name>
  <files>apps/**, packages/** (real lint fixes only)</files>
  <read_first>
    - Output of `bun run lint`
    - Each file named in that output, before editing it
  </read_first>
  <action>
Run `bun run lint`. Fix each error in code (Biome's safe fixes via `bun x @biomejs/biome lint --write` are
allowed; review every change; never `--unsafe` without reading each diff). Do NOT add `biome-ignore`
comments and do NOT turn rules off or down in biome.json. If a finding is a genuine false positive that
cannot be fixed in code, stop and return a checkpoint describing it instead of suppressing it. If the
findings span more than ~40 files, stop after the first coherent batch and return a checkpoint
recommending a split, rather than rushing. Keep behavior identical; re-run
`bun x tsc --noEmit -p apps/next` after fixes. Commit: `fix: resolve Biome 2 lint findings`.

Fresh-install gate (roadmap criterion 3):
`rm -rf node_modules apps/*/node_modules packages/*/node_modules apps/next/.next && bun install --frozen-lockfile && bun run check && bun run turbo:build`.
All must exit 0 and `git status --short` must show no lockfile change afterwards. Also run
`bun run --cwd packages/api validate` and `bun run --cwd apps/next lint` to confirm the updated package
scripts work. Record outputs in the SUMMARY.
  </action>
  <verify>
    <automated>bun run check && test "$(git ls-files apps packages | xargs grep -l 'biome-ignore' 2>/dev/null | wc -l | tr -d ' ')" = 0 && bun run turbo:build && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `bun run check` exits 0
    - `git ls-files apps packages | xargs grep -l biome-ignore | wc -l` is 0 (baseline 0)
    - `git diff <Task1 commit> -- biome.json` shows no rule set to "off"/"warn" relative to the migrate output
    - After wiping node_modules: `bun install --frozen-lockfile`, `bun run check`, `bun run turbo:build` all exit 0 and bun.lock is unchanged
    - Lint fixes are in their own commit, separate from the Task 2 churn commit
  </acceptance_criteria>
  <done>Lint gate is clean and trustworthy from a fresh install.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> repo | Biome 2 packages and platform binaries fetched by bun |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `bun add @biomejs/biome@^2` | accept | Major bump of a package already declared and in use (`@biomejs/biome`, official Biome org); its darwin binary 2.4.13 already exists in node_modules today. No new package names; lockfile diff reviewed to contain only `@biomejs/*` entries |
| T-01-11 | Tampering | generated drizzle migrations | mitigate | Excluded in biome.json `files.includes`; churn commit checked to contain no `src/db/drizzle/` paths |
| T-01-12 | Repudiation | lint suppression hiding real issues | mitigate | Zero `biome-ignore` comments and no rule downgrades are acceptance criteria |
</threat_model>

<verification>
- Fresh-install `bun install --frozen-lockfile && bun run check && bun run turbo:build` all exit 0.
- Three separate commits: upgrade, churn, lint fixes.
</verification>

<success_criteria>
FND-04 done; roadmap success criterion 3 holds locally.
</success_criteria>

<output>
Create `.planning/phases/BFC-01-foundation-repair/01-04-SUMMARY.md` when done.
</output>
