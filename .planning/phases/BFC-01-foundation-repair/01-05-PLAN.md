---
phase: 01-foundation-repair
plan: 05
type: execute
wave: 5
depends_on: ["01-04"]
files_modified: []
autonomous: false
requirements: [FND-01, FND-05]

must_haves:
  truths:
    - "D-11: The user (not Claude) deploys this branch's HEAD to production - Vercel web and `wrangler deploy` API"
    - "D-11 / FND-01: Signed in on https://www.befocus.work, the user sees their own work duration, break duration and session count on first paint, with no flash of 25:00 / 5:00"
    - "D-11 / FND-05: The browser network panel shows zero client requests to /user/settings on dashboard load, and timer values never change after first paint"
    - "Production serves CSS with a single `--border` per scope and a real `@keyframes shine`, and /guest still renders"
  artifacts: []
  key_links:
    - from: "https://www.befocus.work (Vercel)"
      to: "production API (Cloudflare Worker)"
      via: "getUserSettings forwarding `__Secure-better-auth.session_token` in the raw Cookie header"
      pattern: "__Secure-better-auth.session_token"
---

## Phase Goal

The dashboard receives a signed-in user's saved settings on the server render in production, from
exactly one hydration path, and the styling and lint toolchain is single-sourced and trustworthy again.

<objective>
Slice: the fix is proven where the bug actually lives - production, with secure cookies scoped to
befocus.work. Vercel preview URLs (`*.vercel.app`) cannot test this because auth cookies are scoped to
befocus.work (D-11).

Purpose: FND-01 is only verifiable against the live deployment; FND-05's one-read claim is re-checked there.
Output: user-confirmed production verification recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/BFC-01-foundation-repair/01-CONTEXT.md
@.planning/phases/BFC-01-foundation-repair/01-03-SUMMARY.md
@.planning/phases/BFC-01-foundation-repair/01-04-SUMMARY.md
@.planning/codebase/INTEGRATIONS.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User deploys web and API to production (D-11)</name>
  <files>(none)</files>
  <read_first>
    - .planning/codebase/INTEGRATIONS.md (deployment targets)
    - .planning/phases/BFC-01-foundation-repair/01-04-SUMMARY.md (fresh-install gate passed)
  </read_first>
  <action>
Claude does NOT run any deploy command (D-11 locked decision). Before pausing, Claude runs
`git log --oneline -8` and `git status --short` and shows the user the exact commit to deploy and that the
tree is clean (ignoring the pre-existing unrelated `package-lock.json` change). Then present:
1. Deploy branch `feat/customize-v2` at the shown commit to Vercel production for https://www.befocus.work
   (however you normally promote to production).
2. `cd packages/api && bun run deploy` (wrangler deploy --minify) for the production API.
3. Reply with "deployed" plus the Vercel deployment URL/ID.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w '%{http_code}' https://www.befocus.work/guest | grep -q 200 && echo DEPLOY_REACHABLE</automated>
  </verify>
  <acceptance_criteria>
    - User replied "deployed" with a deployment identifier
    - https://www.befocus.work/guest returns 200
  </acceptance_criteria>
  <resume-signal>Type "deployed" with the Vercel deployment URL/ID, or describe any deploy failure</resume-signal>
  <done>Phase 1 code is live on production web and API.</done>
</task>

<task type="auto">
  <name>Task 2: Automated production smoke checks</name>
  <files>(none - verification only)</files>
  <read_first>
    - .planning/phases/BFC-01-foundation-repair/01-02-SUMMARY.md (expected CSS facts)
  </read_first>
  <action>
Without any credentials:
1. `curl -s https://www.befocus.work/guest` -> 200; extract the linked `/_next/static/css/*.css` URLs from
   the HTML and fetch them.
2. In the fetched CSS: exactly two `--border:` values (`0 0% 69%` and `0 0% 25%`), at least one
   `@keyframes shine`, no `@theme`.
3. `curl -s -o /dev/null -w '%{http_code}' https://www.befocus.work/` without a cookie -> redirect to
   /guest (middleware unchanged).
Record results in the SUMMARY. Never request or store user cookies.
  </action>
  <verify>
    <automated>curl -s https://www.befocus.work/guest | grep -o '/_next/static/css/[^"]*\.css' | sort -u | sed 's#^#https://www.befocus.work#' | xargs curl -s | grep -q '@keyframes shine' && echo PROD_CSS_OK</automated>
  </verify>
  <acceptance_criteria>
    - /guest returns 200
    - Production CSS has exactly two `--border:` values (`0 0% 69%`, `0 0% 25%`), contains `@keyframes shine`, contains no `@theme`
    - / without cookie redirects to /guest
  </acceptance_criteria>
  <done>Production serves the repaired CSS and routes behave as before.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User verifies signed-in first paint and single settings read in production (D-11)</name>
  <files>(none)</files>
  <read_first>
    - .planning/phases/BFC-01-foundation-repair/01-03-SUMMARY.md (what was proven locally)
  </read_first>
  <action>Claude presents the steps below and waits; it does not access the user's account.</action>
  <what-built>Secure-cookie-safe server settings fetch, single flash-free hydration path, single-sourced Tailwind/tokens, Biome 2 gate - now deployed.</what-built>
  <how-to-verify>
    1. Sign in at https://www.befocus.work with an account whose saved work duration is NOT 25 min (change it in session settings and save first if needed).
    2. Open DevTools -> Network, enable "Disable cache", filter by `settings`, then hard-reload the dashboard.
    3. Expect: the timer shows YOUR work duration from the very first frame - no 25:00 that then changes. Break duration and session count in the session UI match your saved values.
    4. Expect: zero requests to `/user/settings` in the browser Network panel on load (the one read happens on the server). Switch to another tab and back: still zero.
    5. Wait 10 seconds without touching anything: timer values do not change.
    6. Change a setting and save: success toast; hard-reload shows the new value on first paint.
    7. Sign out, open https://www.befocus.work/guest: timer shows 25:00 defaults and the page looks exactly as before (borders, colours, layout).
  </how-to-verify>
  <verify>
    <automated>MISSING - production signed-in behaviour requires the user's own authenticated browser session; Task 2 covers the unauthenticated automated checks</automated>
  </verify>
  <acceptance_criteria>
    - User confirms steps 3-7 all matched expectations
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe which step differed</resume-signal>
  <done>FND-01 verified on the live deployment; phase success criteria 1-2 confirmed in production.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude -> production | Claude only makes unauthenticated GET requests to public pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | Elevation of Privilege | production deploy | mitigate | Deploy is a blocking human-action checkpoint; Claude runs no deploy or wrangler publish command (D-11) |
| T-01-14 | Information Disclosure | user session in production verification | mitigate | Signed-in checks are done by the user in their own browser; Claude never requests, receives or stores production cookies |
</threat_model>

<verification>
User approval of Task 3 plus Task 2 automated checks.
</verification>

<success_criteria>
Roadmap success criteria 1, 2 and 4 hold in production; criterion 3 held in 01-04.
</success_criteria>

<output>
Create `.planning/phases/BFC-01-foundation-repair/01-05-SUMMARY.md` when done.
</output>
